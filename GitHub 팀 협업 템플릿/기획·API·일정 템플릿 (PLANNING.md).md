> **용도:** 프로젝트의 요구사항·화면·API·DB·일정·결정사항을 한곳에 모으는 템플릿입니다.  
> 저장소에서는 `docs/PLANNING.md`로 사용할 수 있습니다.

# 1. 프로젝트 개요

## 한 줄 설명
{{누가 어떤 문제를 해결하는 서비스인지}}

## 목표
- {{목표 1}}
- {{목표 2}}

## 이번 프로젝트에서 하지 않는 것
- {{범위 제외 1}}
- {{범위 제외 2}}

---

# 2. 사용자와 핵심 흐름

## 주요 사용자
- {{사용자 유형}}

## 핵심 사용자 흐름

```text
예:
회원가입
→ 로그인
→ 강의 검색
→ 강의 상세
→ 수강 신청
→ 마이페이지 확인
```

---

# 3. 기능 목록과 우선순위

| ID | 기능 | 설명 | 우선순위 | 담당 | 상태 |
| --- | --- | --- | --- | --- | --- |
| F-01 | {{기능}} | {{설명}} | Must | {{담당}} | TODO |
| F-02 | {{기능}} | {{설명}} | Should | {{담당}} | TODO |

우선순위 예:

- Must: 없으면 서비스가 성립하지 않음
- Should: 중요하지만 일정에 따라 조정 가능
- Could: 시간이 남으면
- Won't: 이번 범위에서 제외

---

# 4. 화면 설계

| 화면 | 경로 | 주요 기능 | 사용하는 API |
| --- | --- | --- | --- |
| {{로그인}} | `/login` | {{입력/로그인}} | `POST /auth/login` |
| {{목록}} | `/courses` | {{조회/검색}} | `GET /courses` |

필요하면 Figma/Notion 화면 링크를 추가합니다.

---

# 5. API 계약

## `{{METHOD}} {{/path}}`

### 목적
{{이 API가 하는 일}}

### 인증
{{필요 / 불필요}}

### Request

```json
{
  "example": "value"
}
```

### Success Response

```json
{
  "data": {}
}
```

### Error Response

```json
{
  "error": {
    "code": "ERROR_CODE",
    "message": "설명"
  }
}
```

### 상태 코드
- `200`: {{설명}}
- `400`: {{설명}}
- `401`: {{설명}}

### 담당
- FE: {{담당}}
- BE: {{담당}}

---

# 6. 데이터베이스 / ERD

## 핵심 엔티티

| 테이블 | 역할 | 주요 컬럼 |
| --- | --- | --- |
| `users` | 사용자 | `id`, `email`, `password_hash` |
| `{{table}}` | {{역할}} | {{컬럼}} |

## 관계

```text
users 1 ─── N enrollments N ─── 1 courses
```

DB 구조가 바뀌면 API와 프론트 영향 범위를 함께 기록합니다.

---

# 7. 환경변수

실제 값은 적지 않습니다.

```text
DATABASE_URL=
SECRET_KEY=
AI_API_KEY=
FRONTEND_ORIGIN=
```

환경변수가 추가되면 `.env.example`도 갱신합니다.

---

# 8. 기술 스택

| 영역 | 기술 | 선택 이유 |
| --- | --- | --- |
| Frontend | {{React 등}} | {{이유}} |
| Backend | {{Flask 등}} | {{이유}} |
| DB | {{MySQL 등}} | {{이유}} |
| AI | {{사용 시}} | {{이유}} |
| Deploy | {{AWS 등}} | {{이유}} |

---

# 9. 일정

| 날짜/스프린트 | 목표 | 완료 조건 |
| --- | --- | --- |
| {{1일차}} | {{환경 + 기본 화면}} | {{조건}} |
| {{2일차}} | {{API 연결}} | {{조건}} |
| {{3일차}} | {{통합 + 배포}} | {{조건}} |

---

# 10. 결정 기록

큰 결정은 이유를 남깁니다.

## {{날짜}} — {{결정 제목}}

### 문제
{{무엇을 결정해야 했는가}}

### 선택지
1. {{A}}
2. {{B}}

### 결정
{{선택}}

### 이유
{{왜 선택했는가}}

### 영향
{{어떤 코드/팀에 영향이 있는가}}

---

# 11. 미해결 사항

- [ ] {{확인할 것}}
- [ ] {{팀과 합의할 것}}
- [ ] {{외부 문서 확인}}

---

# 12. 배포 정보

```text
Frontend URL:
Backend URL:
API Base URL:
DB:
배포 브랜치:
```

비밀번호·키·보안그룹 상세값 등 민감한 정보는 이 문서에 넣지 않습니다.
