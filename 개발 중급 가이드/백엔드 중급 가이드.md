> **대상:** Route → Service → Model 구조로 API 서버 하나(인증, CRUD, 배포)를 끝까지 만들어본 사람
> **목적:** 요청이 몰리거나 데이터가 커졌을 때 실제로 부딪히는 문제와 대응 방법을 정리합니다

---

# 0. 시작 전에 — 자주 나오는 용어

기초 가이드에서 다룬 Route/Service/Model, HTTP, REST, 인증·인가 용어는 이미 안다고 가정합니다. 여기서는 이 문서에서 새로 나오는 용어만 정리합니다.

| 용어 | 쉬운 설명 |
| --- | --- |
| 캐싱(Caching) | 자주 조회되지만 잘 안 바뀌는 데이터를 미리 빠른 저장소(Redis 등)에 저장해뒀다 재사용하는 것 |
| 캐시 무효화(Cache Invalidation) | 원본 데이터가 바뀌었을 때, 저장해둔 캐시를 지우거나 갱신해서 옛날 값을 안 보여주게 하는 것 |
| TTL(Time To Live) | 캐시가 자동으로 사라지기까지의 유효 시간 |
| 메시지 큐(Message Queue) | "지금 당장 처리 안 해도 되는 작업"을 줄 세워뒀다가 별도 프로세스가 순서대로 처리하게 하는 장치 |
| 워커(Worker) | 메시지 큐에 쌓인 작업을 실제로 꺼내서 처리하는 별도 프로세스 |
| N+1 쿼리 문제 | 목록 하나를 조회할 때, 항목마다 연관 데이터를 별도 쿼리로 매번 가져와 쿼리 수가 크게 늘어나는 문제 |
| Eager Loading | N+1을 막기 위해, 연관 데이터를 처음부터 한 번의 쿼리로 함께 가져오는 방식 |
| API 버저닝(API Versioning) | API 응답 형식이 바뀌어도 기존 클라이언트가 계속 동작하도록 버전을 나눠 관리하는 것 |
| Rate Limiting | 한 사용자·IP가 짧은 시간에 너무 많이 요청하지 못하도록 횟수를 제한하는 것 |
| 구조화 로깅(Structured Logging) | 검색·필터링이 가능하도록 로그를 JSON 같은 정해진 형식으로 남기는 방식 |
| APM (Application Performance Monitoring) | 요청마다 걸린 시간과 느린 지점을 자동으로 추적·기록해주는 모니터링 도구 |
| 트랜잭션 격리 수준(Isolation Level) | 동시에 실행되는 여러 트랜잭션이 서로의 변경을 어디까지 볼 수 있는지 정하는 DB 설정 |
| 수평 확장(Scale Out) | 서버 사양을 올리는 대신 서버 대수를 늘려서 트래픽을 감당하는 방식 |
| 무상태(Stateless) | 서버가 사용자 상태(로그인 정보 등)를 자기 메모리에 담아두지 않는 설계. 어느 서버가 요청을 받아도 동일하게 처리 가능해짐 |
| 로드밸런서(Load Balancer) | 들어오는 요청을 여러 서버에 골고루 나눠주는 장치 |

---

# 1. 캐싱 전략

## 언제 캐싱이 필요한가

같은 조회 요청이 반복되고, 결과가 자주 바뀌지 않을 때 씁니다. 예: 인기 강의 목록, 카테고리 목록.

```python
import redis
import json

r = redis.Redis()

def get_popular_courses():
    cached = r.get("popular_courses")
    if cached:
        return json.loads(cached)

    courses = Course.query.order_by(Course.enrollment_count.desc()).limit(10).all()
    result = [c.to_dict() for c in courses]
    r.setex("popular_courses", 300, json.dumps(result))  # 300초(5분) 뒤 자동 만료
    return result
```

## 캐시 무효화 패턴

| 전략 | 설명 |
| --- | --- |
| TTL(Time To Live) | 일정 시간 뒤 자동 만료 — 구현이 가장 간단함 |
| 쓰기 시 무효화 | 데이터가 바뀌는 순간 관련 캐시를 직접 삭제 |
| 캐시 키에 버전 포함 | 데이터가 바뀌면 버전을 올려서 이전 캐시를 자연스럽게 못 쓰게 만듦 |

**기본 상식**: "캐시 무효화와 이름 짓기가 컴퓨터과학에서 가장 어려운 두 가지 문제"라는 말이 있을 만큼, 언제 캐시를 지울지가 캐싱 자체보다 어렵습니다. 처음엔 TTL만으로 시작하고, 데이터 정합성이 중요해지면 쓰기 시 무효화를 추가하는 순서를 권장합니다.

---

# 2. 메시지 큐 — 비동기 처리

이메일 발송, 썸네일 생성처럼 시간이 걸리지만 즉시 응답할 필요는 없는 작업을 요청-응답 흐름에서 분리합니다.

```text
사용자 요청 → 서버가 "작업 등록"만 하고 즉시 응답
                ↓
            메시지 큐(Redis, RabbitMQ 등)
                ↓
            별도의 워커(worker) 프로세스가 실제 작업을 처리
```

<svg viewBox="0 0 700 220" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="서버가 작업을 메시지 큐에 등록하고 즉시 응답한 뒤, 별도 워커가 큐에서 꺼내 비동기로 처리하는 구조도" style="max-width:100%;height:auto;font-family:inherit">
  <style>
    .mq-server{fill:var(--callout-concept-bg);stroke:var(--accent);stroke-width:1.5}
    .mq-queue{fill:var(--accent-weak);stroke:var(--accent);stroke-width:2}
    .mq-worker{fill:var(--surface-soft-2);stroke:var(--border-strong);stroke-width:1.5}
    .mq-text{fill:var(--text);font-size:12px;text-anchor:middle}
    .mq-sub{fill:var(--text-secondary);font-size:10px;text-anchor:middle}
    .mq-arrow{stroke:var(--text-faint);stroke-width:1.5;fill:none;marker-end:url(#mqArrow)}
  </style>
  <defs>
    <marker id="mqArrow" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M0,0 L10,5 L0,10 z" fill="var(--text-faint)"/>
    </marker>
  </defs>

  <rect x="20" y="80" width="130" height="50" rx="8" class="mq-server"/>
  <text x="85" y="103" class="mq-text" style="font-weight:700">서버</text>
  <text x="85" y="119" class="mq-sub">작업 등록 후 즉시 응답</text>

  <line x1="150" y1="105" x2="260" y2="105" class="mq-arrow"/>
  <text x="205" y="95" class="mq-sub">등록</text>
  <rect x="260" y="80" width="160" height="50" rx="8" class="mq-queue"/>
  <text x="340" y="103" class="mq-text" style="font-weight:700">메시지 큐</text>
  <text x="340" y="119" class="mq-sub">작업이 순서대로 쌓임</text>

  <line x1="420" y1="105" x2="530" y2="105" class="mq-arrow"/>
  <text x="475" y="95" class="mq-sub">소비</text>
  <rect x="530" y="80" width="150" height="50" rx="8" class="mq-worker"/>
  <text x="605" y="103" class="mq-text" style="font-weight:700">워커</text>
  <text x="605" y="119" class="mq-sub">비동기로 처리</text>

  <line x1="85" y1="130" x2="85" y2="170" class="mq-arrow"/>
  <text x="85" y="185" class="mq-sub">클라이언트에 즉시 응답</text>
</svg>

```python
# Celery 예시
from celery import Celery

celery_app = Celery("tasks", broker="redis://localhost:6379/0")

@celery_app.task
def send_welcome_email(user_id):
    user = User.query.get(user_id)
    mail.send(user.email, "가입을 환영합니다")

# 라우트에서는 이렇게 호출만 하고 바로 응답
send_welcome_email.delay(user.id)
return jsonify({"data": {"id": user.id}}), 201
```

**실무 팁**: 요청-응답 안에서 시간이 오래 걸리는 작업(외부 API 호출, 대용량 파일 처리, 이메일 발송)을 발견하면 "이걸 나중에 처리해도 되는가?"를 먼저 물어봅니다. 그렇다면 큐로 분리해 응답 속도를 개선할 수 있습니다.

---

# 3. N+1 쿼리 문제

목록을 조회할 때, 목록 하나당 연관 데이터를 별도 쿼리로 매번 가져오면 쿼리 수가 N+1개로 늘어납니다.

```python
# 나쁜 예 — courses가 10개면 쿼리가 11번 실행됨 (1 + 10)
courses = Course.query.all()
for c in courses:
    print(c.instructor.name)   # 강사 정보를 매번 별도 쿼리로 조회

# 좋은 예 — eager loading으로 한 번에 가져옴
from sqlalchemy.orm import joinedload
courses = Course.query.options(joinedload(Course.instructor)).all()
```

**확인 방법**: ORM이 실제로 어떤 SQL을 만드는지 로그로 켜두고, 목록 화면에서 쿼리 수가 데이터 개수에 비례해 늘어나는지 확인합니다. (Flask-SQLAlchemy는 `SQLALCHEMY_ECHO=True`로 쿼리 로그를 볼 수 있습니다.)

**주의**: eager loading을 아무 데나 무조건 쓰는 것도 정답은 아닙니다. 매번 필요하지 않은 연관 데이터까지 항상 함께 불러오면 불필요한 조인·데이터 전송이 늘어날 수 있습니다. 실제로 N+1이 발생하고 있는 화면(연관 데이터를 항상 함께 보여주는 목록)에 한해 적용하는 것이 기본 원칙입니다.

---

# 4. API 버저닝

이미 배포된 API의 응답 형식을 바꿔야 할 때, 기존 클라이언트(오래된 앱 버전 등)를 깨뜨리지 않는 방법입니다.

| 방식 | 예시 | 특징 |
| --- | --- | --- |
| URL 경로 | `/api/v1/courses`, `/api/v2/courses` | 가장 명확하고 흔히 씀 |
| 헤더 | `Accept: application/vnd.myapp.v2+json` | URL은 깔끔하지만 덜 직관적 |
| 쿼리 파라미터 | `/api/courses?version=2` | 캐싱과 함께 쓰기 까다로움 |

**기본 상식**: 처음부터 `v1`을 붙여두면, 나중에 `v2`가 필요할 때 기존 URL 구조를 유지한 채 확장할 수 있습니다. 버전을 아예 안 붙였다가 나중에 붙이려면 모든 클라이언트와 조율이 필요해 훨씬 어렵습니다.

---

# 5. Rate Limiting

한 사용자(또는 IP)가 짧은 시간에 너무 많은 요청을 보내는 것을 막습니다. 로그인 시도 무차별 공격, 크롤링 방지에도 쓰입니다.

```python
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address

limiter = Limiter(get_remote_address, app=app, default_limits=["200 per day"])

@app.route("/api/auth/login", methods=["POST"])
@limiter.limit("5 per minute")   # 로그인은 더 엄격하게
def login():
    ...
```

한도를 넘으면 보통 `429 Too Many Requests` 상태 코드로 응답합니다.

---

# 6. 구조화 로깅과 APM 기초

## 구조화 로깅

`print()`나 단순 문자열 로그 대신, 검색·필터링이 가능한 JSON 형태로 남깁니다.

```python
import logging, json

logger = logging.getLogger("app")

def log_event(event, **fields):
    logger.info(json.dumps({"event": event, **fields}))

log_event("course_enrolled", user_id=user.id, course_id=course.id)
# {"event": "course_enrolled", "user_id": 42, "course_id": 7}
```

이렇게 남기면 로그 수집 도구(예: CloudWatch, ELK)에서 "특정 사용자의 이벤트만" 또는 "특정 이벤트가 몇 번 발생했는지" 바로 검색할 수 있습니다.

## APM(Application Performance Monitoring)

요청별로 얼마나 걸렸는지, 어느 함수가 느린지를 자동으로 추적하는 도구입니다(예: Sentry, Datadog). 운영 중 "왜 갑자기 느려졌는지"를 로그만으로 찾기 어려울 때 큰 도움이 됩니다.

---

# 7. 트랜잭션 격리 수준

여러 요청이 동시에 같은 데이터를 건드릴 때 어디까지 서로의 변경을 볼 수 있는지를 정합니다.

| 수준 | 설명 |
| --- | --- |
| Read Uncommitted | 다른 트랜잭션의 커밋 전 변경도 보임 (거의 안 씀) |
| Read Committed | 커밋된 변경만 보임 (대부분 DB의 기본값) |
| Repeatable Read | 같은 트랜잭션 안에서는 항상 같은 결과를 봄 (MySQL 기본값) |
| Serializable | 트랜잭션을 사실상 순차 실행한 것처럼 완전히 격리 (가장 안전하지만 느림) |

**기본 상식**: 격리 수준을 올릴수록 안전하지만 동시 처리 성능이 떨어집니다. 대부분의 서비스는 기본값(Read Committed 또는 Repeatable Read)으로 충분하고, 재고 차감·정산처럼 "동시에 건드리면 안 되는" 로직에만 `SELECT ... FOR UPDATE` 같은 명시적 잠금을 추가로 검토합니다.

---

# 8. 수평 확장과 무상태 설계

서버 한 대로 감당이 안 되면 서버를 여러 대로 늘립니다(수평 확장). 이걸 가능하게 하려면 서버가 **무상태(stateless)** 여야 합니다.

```text
상태를 서버 메모리에 두면 (나쁜 예)
→ 로그인 세션을 서버1의 메모리에 저장
→ 다음 요청이 서버2로 가면 로그인 정보를 못 찾음

무상태로 설계하면 (좋은 예)
→ 로그인 상태를 JWT 토큰(클라이언트가 들고 있음) 또는
  공용 저장소(Redis)에 저장
→ 어느 서버가 요청을 받아도 동일하게 처리 가능
```

<svg viewBox="0 0 700 300" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="수평 확장 아키텍처: 로드밸런서와 여러 서버, 공용 저장소" style="max-width:100%;height:auto;font-family:inherit">
  <style>
    .hs-client{fill:var(--callout-concept-bg);stroke:var(--accent);stroke-width:1.5}
    .hs-lb{fill:var(--accent-weak);stroke:var(--accent);stroke-width:2}
    .hs-server{fill:var(--surface-soft-2);stroke:var(--border-strong);stroke-width:1.5}
    .hs-store{fill:var(--callout-tip-bg);stroke:var(--callout-tip-border);stroke-width:1.5}
    .hs-text{fill:var(--text);font-size:12px;text-anchor:middle}
    .hs-sub{fill:var(--text-secondary);font-size:10px;text-anchor:middle}
    .hs-arrow{stroke:var(--text-faint);stroke-width:1.5;fill:none;marker-end:url(#hsArrow)}
  </style>
  <defs>
    <marker id="hsArrow" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M0,0 L10,5 L0,10 z" fill="var(--text-faint)"/>
    </marker>
  </defs>

  <rect x="290" y="10" width="120" height="40" rx="8" class="hs-client"/>
  <text x="350" y="35" class="hs-text">클라이언트</text>

  <line x1="350" y1="50" x2="350" y2="80" class="hs-arrow"/>
  <rect x="270" y="80" width="160" height="44" rx="8" class="hs-lb"/>
  <text x="350" y="105" class="hs-text" style="font-weight:700">로드밸런서</text>

  <line x1="300" y1="124" x2="150" y2="165" class="hs-arrow"/>
  <line x1="350" y1="124" x2="350" y2="165" class="hs-arrow"/>
  <line x1="400" y1="124" x2="550" y2="165" class="hs-arrow"/>

  <rect x="80" y="165" width="140" height="46" rx="8" class="hs-server"/><text x="150" y="193" class="hs-text">서버 1 (무상태)</text>
  <rect x="280" y="165" width="140" height="46" rx="8" class="hs-server"/><text x="350" y="193" class="hs-text">서버 2 (무상태)</text>
  <rect x="480" y="165" width="140" height="46" rx="8" class="hs-server"/><text x="550" y="193" class="hs-text">서버 3 (무상태)</text>

  <line x1="150" y1="211" x2="330" y2="255" class="hs-arrow"/>
  <line x1="350" y1="211" x2="350" y2="255" class="hs-arrow"/>
  <line x1="550" y1="211" x2="370" y2="255" class="hs-arrow"/>

  <rect x="260" y="255" width="180" height="40" rx="8" class="hs-store"/>
  <text x="350" y="278" class="hs-text">공용 저장소 (Redis / DB)</text>
  <text x="350" y="240" class="hs-sub">세션·상태는 서버가 아니라 여기에</text>
</svg>

로드밸런서가 여러 서버에 요청을 나눠줄 때, 서버마다 결과가 달라지지 않아야 안정적으로 확장할 수 있습니다.

---

# 9. 자주 하는 실수

- 캐시를 넣고 무효화 전략 없이 방치해 오래된 데이터가 계속 보임
- 오래 걸리는 작업(이메일, 파일 처리)을 요청-응답 안에서 동기로 처리해 응답이 느려짐
- 목록 API에서 N+1 쿼리를 방치해 데이터가 늘어날수록 급격히 느려짐
- API 버전 없이 배포하다가 클라이언트 호환성 문제로 롤백
- 세션을 서버 메모리에 저장해 서버를 늘릴 수 없는 구조가 됨

---

# 10. 실전 체크리스트

- [ ] 반복 조회되는 데이터에 캐싱을 검토했는가, 무효화 전략도 함께 정했는가
- [ ] 응답을 오래 지연시키는 작업을 큐로 분리할 수 있는지 확인했는가
- [ ] 목록 API의 쿼리 로그를 확인해 N+1이 없는지 점검했는가
- [ ] 인증처럼 민감한 엔드포인트에 Rate Limiting을 적용했는가
- [ ] 운영 로그가 검색 가능한 구조화 형태로 남는가
- [ ] 세션/상태를 서버 메모리가 아닌 공용 저장소에 두었는가 (확장 가능성 확보)

---

# 11. 멱등성(Idempotency)

같은 요청을 여러 번 보내도 결과가 한 번 보낸 것과 같아야 하는 성질입니다. 네트워크 오류로 클라이언트가 같은 요청을 재시도할 수 있기 때문에 중요합니다.

**주의**: 클라이언트가 요청에 idempotency key를 실어 보내는 것 자체는 멱등성을 만들어주지 않습니다. 그 키를 서버가 저장해두고, 같은 키로 다시 요청이 오면 실제로 식별해서 "이미 처리된 요청이면 저장된 결과를 그대로 돌려주고 작업을 다시 실행하지 않는" 처리를 해야 안전성이 생깁니다. 즉 멱등성을 보장하는 주체는 클라이언트가 아니라 서버입니다.

```python
# 나쁜 예 — 재시도하면 결제가 두 번 될 수 있음
@app.route("/api/payments", methods=["POST"])
def create_payment():
    charge_card(amount)
    return jsonify({"data": {"status": "ok"}}), 201

# 좋은 예 — 서버가 idempotency key를 저장해두고 중복 요청을 직접 식별해서 처리
from sqlalchemy.exc import IntegrityError

@app.route("/api/payments", methods=["POST"])
def create_payment():
    key = request.headers.get("Idempotency-Key")
    existing = Payment.query.filter_by(idempotency_key=key).first()
    if existing:
        return jsonify({"data": existing.to_dict()}), 200

    # idempotency_key 컬럼에 UNIQUE 제약을 걸어둔다.
    # 위의 조회와 아래의 insert 사이에는 시간차가 있어서, 같은 키로 거의 동시에
    # 들어온 두 요청이 둘 다 existing=None을 볼 수 있다. UNIQUE 제약이 없으면
    # 둘 다 결제를 실행해버리는 경쟁 조건(race condition)이 생긴다.
    payment = Payment(idempotency_key=key, status="pending")
    db.session.add(payment)
    try:
        db.session.commit()
    except IntegrityError:
        # 다른 요청이 먼저 같은 키로 커밋한 경우 — 그 결과를 그대로 반환
        db.session.rollback()
        existing = Payment.query.filter_by(idempotency_key=key).first()
        return jsonify({"data": existing.to_dict()}), 200

    charge_card(amount)
    payment.status = "completed"
    db.session.commit()
    return jsonify({"data": payment.to_dict()}), 201
```

**기본 상식**: HTTP 스펙상 `GET`/`PUT`/`DELETE`는 원래 멱등해야 하지만, `POST`는 멱등이 보장되지 않습니다. 결제·주문처럼 중복 실행이 치명적인 `POST` API는 서버가 idempotency key를 저장·조회하는 로직과, 동시 요청에도 중복 저장을 막는 DB 제약(UNIQUE 등)을 함께 구현해야 합니다. 키를 헤더로 받기만 하고 서버가 아무것도 하지 않으면 멱등성은 생기지 않습니다.

---

# 12. 헬스체크와 우아한 종료(Graceful Shutdown)

## 헬스체크

로드밸런서나 오케스트레이터(쿠버네티스 등)가 "이 서버 인스턴스가 정상인지"를 주기적으로 확인하는 엔드포인트입니다.

```python
@app.route("/health")
def health_check():
    try:
        db.session.execute("SELECT 1")   # DB 연결도 함께 확인
        return jsonify({"status": "ok"}), 200
    except Exception:
        return jsonify({"status": "error"}), 503
```

## 우아한 종료

배포로 서버를 내릴 때, 처리 중인 요청을 끝까지 마치고 종료하는 것입니다.

```text
1. 로드밸런서에게 "이 서버는 곧 종료됨"을 알림(헬스체크 실패 처리)
2. 새 요청은 다른 서버로 라우팅되도록 대기
3. 이미 받은 요청을 마저 처리
4. 모두 끝나면 프로세스 종료
```

**기본 상식**: 이 과정 없이 프로세스를 바로 죽이면(`kill -9`), 처리 중이던 요청이 끊겨 사용자에게 오류가 노출됩니다. 대부분의 웹 프레임워크·배포 도구는 `SIGTERM` 신호를 받으면 우아한 종료를 시도하도록 지원합니다.

---

# 13. 배포 전략

| 전략 | 설명 | 장단점 |
| --- | --- | --- |
| 롤링 배포(Rolling) | 서버를 한두 대씩 순서대로 새 버전으로 교체 | 다운타임 없음, 배포 중 신·구 버전이 동시에 존재 |
| 블루-그린(Blue-Green) | 새 버전(Green)을 완전히 띄운 뒤 트래픽을 한 번에 전환 | 롤백이 빠름, 두 배의 서버 자원 필요 |
| 카나리(Canary) | 새 버전에 트래픽 일부(예: 5%)만 먼저 보내 검증 후 점진 확대 | 위험을 최소화, 구성이 더 복잡 |

**실무 팁**: 신·구 버전이 동시에 떠 있는 배포 방식(롤링, 카나리)에서는 DB 마이그레이션도 "구 버전 코드가 여전히 동작하는 스키마"로 먼저 배포하고, 모든 서버가 새 버전으로 바뀐 뒤에 이전 컬럼을 제거하는 식의 하위 호환을 고려해야 합니다.

---

# 14. 동시성 기초 — 스레드/프로세스/비동기

| 모델 | 설명 | 적합한 작업 |
| --- | --- | --- |
| 멀티프로세스 | 프로세스를 여러 개 띄워 CPU 코어를 활용(Gunicorn worker 등) | CPU 연산이 많은 작업 |
| 멀티스레드 | 한 프로세스 안에서 여러 스레드가 동시 처리 | I/O 대기가 많은 작업(단, Python은 GIL로 CPU 병렬성 제한) |
| 비동기(async/await) | I/O 대기 중 다른 작업을 처리하도록 이벤트 루프가 전환 | 외부 API 호출, DB I/O가 많은 작업 |

```python
# 비동기 예시 (FastAPI)
import httpx

@app.get("/api/courses/{id}/reviews-summary")
async def get_summary(id: int):
    async with httpx.AsyncClient() as client:
        res = await client.get(f"https://review-service/api/{id}")
        # await 동안 다른 요청을 처리할 수 있어 서버 전체 처리량이 늘어남
    return res.json()
```

**기본 상식**: Python Flask는 기본적으로 동기 방식이라 프로세스/스레드를 여러 개 띄워 동시성을 확보합니다(Gunicorn `--workers`). 외부 API 호출이 많은 서비스라면 FastAPI 같은 비동기 프레임워크가 같은 서버 자원으로 더 많은 요청을 처리할 수 있습니다.

